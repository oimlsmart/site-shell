/** The type twin for ./host-registry.mjs — see site.d.mts for the pattern. */
export declare const HOST_REGISTRY: readonly {
  readonly key: string
  readonly url: string
  readonly label: string
  readonly desc: string
}[]
